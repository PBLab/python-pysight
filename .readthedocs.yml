version: 2

sphinx:
  configuration: docs/conf.py

requirements_file: docs/requirements.txt

build:
    image: latest

python:
    version: 3.7
    install:
      - method: pip
        path: .
        extra_requirements:
           - docs
      - method: setuptools
        path: .

